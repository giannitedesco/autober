#!/usr/bin/python

import autober
import sys

if __name__ == '__main__':
	for x in sys.argv[1:]:
		lex = autober.lexer(open(x))
		parser = autober.parser(lex)
		p = parser.parse_tree()
		p.pretty_print()
